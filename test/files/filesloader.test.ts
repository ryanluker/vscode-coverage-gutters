import * as assert from "assert";
import {FilesLoader} from "../../src/files/filesloader";

suite("FilesLoader Tests", function() {
    test("loadDataFiles takes file paths and fetches their data strings @unit", function() {
        assert.equal(true, true);
    });

    test("findCoverageFiles finds files in the workspaces @unit", function() {
        assert.equal(true, true);
    });
});

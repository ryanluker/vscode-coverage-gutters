import * as assert from "assert";
import {CoverageParser} from "../../src/files/coverageparser";

suite("CoverageParser Tests", function() {
    test("filesToSections Converts data files to section data structures @unit", function() {
        assert.equal(true, true);
    });

    test("filesToSections Correctly chooses the coverage format @unit", function() {
        assert.equal(true, true);
    });
});
